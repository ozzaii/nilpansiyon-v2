---
target: nil pansiyon — post-fix verification
total_score: 34
p0_count: 0
p1_count: 0
timestamp: 2026-05-13T21-34-50Z
slug: index-html
---
# nil pansiyon — post-fix verification

**Target:** `/Users/ozai/projects/nilpansiyon-v2/index.html`
**Date:** 2026-05-14
**Mode:** verification after debloat + sexify pipeline

## Deltas (from baseline 2026-05-13 run)

| Metric | Before | After | Δ |
|---|---|---|---|
| Lines | 7418 | 6639 | **-779 (-10.5%)** |
| Inline JS (kB) | 97 | 77 | **-20 (-21%)** |
| CSS rules | 1147 | 929 | **-218 (-19%)** |
| Body root children | 33 | 25 | -8 |
| Sections | 12 | 11 | awards cut |
| Detector findings | 12 | 1 | **-91%** |
| Horizontal overflow | 0 | 0 | clean ✓ |
| Console errors (load) | n/a | 0 | clean ✓ |

## Cuts Landed

- Custom cursor system (dot/ring/halo/label + state classes) entirely
- Gilt-frame edge brackets
- `.meander-bg / .dots-bg / .kilim-bg` ornament backgrounds
- Fake awards marquee (Condé Nast et al)
- Ambient WebAudio synth + toggle (140 lines)
- Weather particles canvas + tint (chip + temp + condition kept)
- Hero 4-photo crossfade + 3D tilt → single committed photo
- Hero atmospheric stack: 4 corner brackets + hero-crest SVG + 7 needle spans
- Hero footer-row: pansiyon-stamp + coords (kept scroll-cue)
- Falling spruce-needles canvas
- 3D card tilt (rooms/seasons/testimonials)
- Stats counter slot-machine shuffle → fade-up
- Letter-split per-char → word-level reveal (-90% DOM)

## Perf Fixes

- 9 layout-property transitions → transform / scaleX / translateX
- Boot screen 2.15s → first-visit gated via sessionStorage (subsequent loads instant)
- `--ease-spring` renamed to `--ease-expo` (matches actual cubic-bezier curve)

## Copy / Clarity

- Killed "son rezervasyon 12 dk önce" fake-urgency
- Replaced sticky bar with "*Bu hafta sonu açığız* · WhatsApp bir mesaj uzakta"
- Dropped verbal "Bölüm İki/Üç/Dört/Beş/Altı/Ek" — Roman numerals as single system
- Room reveal-link CTA now visible on touch (was hover-only)
- Price anchor "Gece ~ 4.500 ₺'den · iki kişi · kahvaltı dahil" near rez form (TR + EN)

## Typeset / Color

- Merged `--gold` and `--brass` tokens into single `--brass`/`--brass-hi`/`--brass-deep`
- Bordo (`#5C1F22`) accent pushed into:
  - Konum map main pin (was brass)
  - Hero sub pip (alternating pip dots)
  - Rezervasyon price anchor em
- `--ease-spring` → `--ease-expo` rename

## Bolder Moves

- **Hero declutter**: down from 18 elements to photo + title + tagline + sub + scroll-cue
- **Deckle dividers**: `clamp(48-96)` → `clamp(120-240)` — paper tear that actually reads as a section transition
- **Rooms asymmetric mobile**: staggered grid columns (1/5, 2/-1, 1/5, 2/-1) instead of vanilla single-column stack
- **Konum SVG map**: promoted to full-bleed background, panel rests on top as glass card with backdrop-filter

## Animate

- Boot screen timing 1800/2900 → 1200/2000 ms (faster perceived load)
- Lens reveal now drives on mobile via DeviceOrientationEvent (iOS 13+ permission handled, silent feature-detect, activates after first touch)

## Delight

- Footer signature replaced anonymous squiggle with stylized "Nil" + "Ailesi" handwritten SVG paths (two-path JS-driven stroke-dasharray draw, staggered)
- `.sig-attr` caption added: "Nil Ailesi · *Uzungöl, est 1987*" — italic display name + mono date

## Open

- Heritage physical-proof artifact (1987 register page / receipt) — needs real asset, deferred until Kaan has source material
- AR + RU translations — scaffold/stubs in place, content not yet generated

## Verdict

Aesthetic-minimalism heuristic (1/4 baseline) projected to 3-4/4 — site no longer talks over itself.
Consistency (2/4) lifted via token merge + numbering unification.
Console clean, 0 horizontal overflow, detector at 1/29 (intentional Fraunces flag remains).
